// SIGNAL ARENA — базовые токены интерфейса.
// Одно место для шкал: типографика, отступы, радиусы, тайминги, тач-зоны.
// Правило: в сценах НЕТ инлайновых размеров шрифта и магических отступов.

/** Ширина/высота логического холста (портретный телефон). */
export const CANVAS = { w: 390, h: 844 } as const;

/**
 * Типографическая шкала. Минимум 12px — ниже текст физически нечитаем
 * на телефоне при холсте 390pt (аудит R1).
 */
export const FS = {
  /** 12 — подписи, служебные метки. Абсолютный минимум. */
  caption: 12,
  /** 14 — вторичный текст, подсказки, значения. */
  body: 14,
  /** 16 — основной текст: варианты ответа, пункты списков. */
  bodyLg: 16,
  /** 20 — заголовки блоков и экранов. */
  title: 20,
  /** 28 — крупные акценты: вердикт, цифры результата. */
  display: 28,
} as const;

/** Межстрочный интервал для многострочных блоков. */
export const LINE = {
  tight: 1.15,
  normal: 1.35,
  loose: 1.5,
} as const;

/** Сетка отступов 4/8/12/16/24/32 (аудит R3 — вместо магических координат). */
export const SP = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const;

/** Горизонтальные поля экрана. */
export const GUTTER = SP.lg;
/** Ширина контента между полями. */
export const CONTENT_W = CANVAS.w - GUTTER * 2;

/** Радиусы скруглений по эпохам задаются палитрой; это базовые значения. */
export const RADIUS = {
  sm: 6,
  md: 10,
  lg: 14,
  pill: 999,
} as const;

/**
 * Минимальная интерактивная область. Apple HIG — 44pt, Material — 48dp.
 * Визуал может быть меньше, но hit-area расширяется до этого значения (аудит T1).
 */
export const HIT = {
  min: 44,
  comfortable: 48,
} as const;

/** Длительности анимаций — единый ритм интерфейса (аудит AN1). */
export const DUR = {
  /** Отклик на касание. */
  tap: 120,
  /** Появление/скрытие панели. */
  panel: 240,
  /** Переход между экранами. */
  scene: 180,
  /** Тик счётчика (XP/SIG). */
  counter: 500,
  /** Одна свеча в «проигрыше вперёд». */
  candle: 200,
} as const;

/** Кривые сглаживания. */
export const EASE = {
  out: 'Cubic.easeOut',
  inOut: 'Cubic.easeInOut',
  back: 'Back.easeOut',
} as const;

/** Высоты постоянных элементов каркаса. */
export const CHROME = {
  topBar: 64,
  bottomNav: 64,
} as const;
